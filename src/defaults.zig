/// window width value.
pub const window_width = 800;
/// window height value.
pub const window_height = 540;
/// fps count.
pub const fps = 60;
/// exit, a is actually q on my french keyboard
pub const exit_key = 'a';
/// number of entries to read
pub const cmd_entries_cnt = 103;
/// time from which to read the data
pub const time_init = 0;
/// number of entries show at a time on the list widget
pub const max_entries_shown_list = 8;
/// main hist file
pub const main_hist_file = "~/.zsh_history_backup";
